__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
.eggs/
*.egg
.pytest_cache/
.ruff_cache/
.pyright/
.venv-mps/
.venv-mlx/
.worktrees/

# Per-run training artifacts (see examples/fit_image.py — runs/<timestamp>/).
# Reproducible from CLI + config.json; not source.
runs/

# Validation script artifacts (scripts/validate_winner_astronaut.py).
# Numbers and recon PNGs are posted in PR comments, not tracked in the repo.
validation_astronaut/

# Claude Code ephemeral state
.claude/worktrees/
.superpowers/
